# Source => integer result, or error:<diagnostic>. LONG_MIN is computed within range.
len(moving_average_sum([], 1)) => 0
len(moving_average_sum([1], 2)) => 0
moving_average_sum([{LONG_MAX}, 0], 2)[0] => {HALF_LONG_MAX}
moving_average_sum([{LONG_MAX}, 1], 2)[0] => error:integer overflow
moving_average_sum([1, {LONG_MAX}, -1], 3)[0] => error:integer overflow
let low = 0 - {LONG_MAX} - 1; moving_average_sum([low, -1], 2)[0] => error:integer overflow
let low = 0 - {LONG_MAX} - 1; moving_average_sum([low, 0], 2)[0] => {HALF_LONG_MIN}
moving_average_sum([-3, 2], 2)[0] => 0
moving_average_sum([0, {LONG_MAX}, 1], 2)[1] => error:integer overflow
moving_average_sum(push([{LONG_MAX}], 1), 2)[0] => error:integer overflow
fn avg(xs) { moving_average_sum(xs, 2)[0] }; avg([2, 3]) => 2
if 0 { moving_average_sum([{LONG_MAX}, 1], 2)[0] } else { 7 } => 7
0 && moving_average_sum([{LONG_MAX}, 1], 2)[0] => 0
moving_average_sum(1, 2) => error:expected array
moving_average_sum([1], 0) => error:expected integer
moving_average_sum([1], 1, 2) => error:wrong argument count
let n = 0; let total = 0; while n < 65 { total = total + moving_average_sum(push([1], 2), 2)[0]; n = n + 1; }; total => 65
