# Source => integer result, or error:<diagnostic>. LONG_MIN is computed within range.
len(prefix_sum([])) => 0
prefix_sum([{LONG_MAX}, 0])[1] => {LONG_MAX}
prefix_sum([{LONG_MAX}, 1])[1] => error:integer overflow
prefix_sum([1, {LONG_MAX}, -1])[2] => error:integer overflow
let low = 0 - {LONG_MAX} - 1; prefix_sum([low, -1])[1] => error:integer overflow
let low = 0 - {LONG_MAX} - 1; prefix_sum([low, 0])[1] => {LONG_MIN}
sum(prefix_sum([2, 3, 5])) => 17
prefix_sum(push([{LONG_MAX}], 1))[1] => error:integer overflow
fn last(xs) { prefix_sum(xs)[1] }; last([2, 3]) => 5
if 0 { prefix_sum([{LONG_MAX}, 1])[1] } else { 7 } => 7
0 && prefix_sum([{LONG_MAX}, 1])[1] => 0
prefix_sum([1], [2]) => error:wrong argument count
prefix_sum(1) => error:expected array
let n = 0; let total = 0; while n < 65 { total = total + prefix_sum(push([1], 2))[1]; n = n + 1; }; total => 195
