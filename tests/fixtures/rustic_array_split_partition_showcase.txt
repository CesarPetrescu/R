# Rustic bounded split/partition helper showcase
sum(drop(range(6), 3)) => 12
fn odd(x) { x % 2 == 1 }; partition_count(drop(range(8), 2), odd) => 3
fn keep(x) { x > 1 }; let xs = difference(drop([0, 1, 2, 3, 4], 1), [3]); partition_count(xs, keep) * 100 + sum(xs) => 207
